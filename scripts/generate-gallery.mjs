import fs from "node:fs";import path from "node:path";
const root=path.resolve("images/gallery");const output=path.resolve("data/gallery.json");const extensions=new Set([".jpg",".jpeg",".png",".webp",".avif"]);
function titleFromName(name){return name.replace(/\.[^.]+$/,"").replace(/[-_]+/g," ").replace(/\s+/g," ").trim().replace(/\b\w/g,c=>c.toUpperCase())}
function walk(dir){if(!fs.existsSync(dir))return [];return fs.readdirSync(dir,{withFileTypes:true}).filter(e=>e.isFile()&&extensions.has(path.extname(e.name).toLowerCase())).sort((a,b)=>a.name.localeCompare(b.name,undefined,{numeric:true,sensitivity:"base"})).map(e=>e.name)}
function build(){if(!fs.existsSync(root))fs.mkdirSync(root,{recursive:true});const albums=[];for(const entry of fs.readdirSync(root,{withFileTypes:true}).filter(e=>e.isDirectory()).sort((a,b)=>a.name.localeCompare(b.name,undefined,{sensitivity:"base"}))){const dir=path.join(root,entry.name);const images=walk(dir).map(file=>({src:"images/gallery/"+entry.name+"/"+file,alt:titleFromName(file),title:titleFromName(file)}));if(images.length)albums.push({id:entry.name.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,""),title:titleFromName(entry.name),images})}fs.mkdirSync(path.dirname(output),{recursive:true});fs.writeFileSync(output,JSON.stringify(albums,null,2)+"\n");console.log("Generated",output,"with",albums.length,"albums");}
build();